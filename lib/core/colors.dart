import 'package:flutter/material.dart';

class HannamiColors {
  const HannamiColors._();

  static const Color background = Color(0xFF0D0D0D); // pure black-ish
  static const Color cardBackground = Color(0xFF1A1A1A);
  static const Color primary = Color(0xFF70A6FF);
  static const Color secondary = Color(0xFF5E8FFF);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
}
